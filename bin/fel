#!/usr/bin/env ruby
require 'frontendloader'
fel = FrontendLoader.new
case ARGV[0]
when "init"
  fel.init_app
when "compile"
  fel.compile 
when "listen"
  fel.listen
when "watch"
  fel.listen
when "--watch"
  fel.listen
end